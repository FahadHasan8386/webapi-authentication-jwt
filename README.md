# webapi-authentication-jwt
This project demonstrates how to implement JWT (JSON Web Token) authentication in ASP.NET Core Web API. It includes user login, token generation, token validation, and protected API endpoints. The project follows a clean architecture structure with Controller, Service, and Repository layers.
